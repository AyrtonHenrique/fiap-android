<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fiap.fiap_android_seguros">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.usuario.PesquisarPlanosActivity"></activity>
        <activity android:name=".ui.corretor.PesquisarClientesActivity" />
        <activity android:name=".ui.corretor.MeusClientesActivity" />
        <activity android:name=".ui.corretor.FalarClienteActivity" />
        <activity android:name=".ui.corretor.CorretorActivity" />
        <activity android:name=".ui.sobre.SobreActivity" />
        <activity android:name=".ui.usuario.FalarCorretorActivity" />
        <activity android:name=".ui.mensagens.MensagensEnviadasActivity" />
        <activity android:name=".ui.usuario.UsuarioActivity" />
        <activity android:name=".ui.usuario.NovoCadastroActivity" />
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.MainActivity" />
    </application>

</manifest>